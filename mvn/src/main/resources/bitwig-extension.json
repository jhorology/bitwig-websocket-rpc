{
  "name": "${extension.name}",
  "author" : "${extension.author}",
  "version": "${extension.version}",
  "id": "${extension.uuid}",
  "requiredAPIVersion": ${extension.requiredAPIVersion},
  "usingBetaAPI": ${extension.usingBetaAPI},
  "shouldFailOnDeprecatedUse": ${extension.shouldFailOnDeprecatedUse},
  "errorReportingEMail": "${extension.errorReportingEMail}",
  "hardwareVendor": "${extension.hardwareVendor}",
  "hardwareModel": "${extension.hardwareModel}",
  "defaultConfig": {
    "logLevel": "${extension.config.logLevel}",
    "logOutputSystemConsole": ${extension.config.logOutputSystemConsole},
    "webSocketPort": ${extension.config.webSocketPort}
  }
}
